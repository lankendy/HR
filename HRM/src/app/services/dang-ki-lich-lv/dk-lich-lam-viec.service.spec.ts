/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DkLichLamViecService } from './dk-lich-lam-viec.service';

describe('Service: DkLichLamViec', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DkLichLamViecService]
    });
  });

  it('should ...', inject([DkLichLamViecService], (service: DkLichLamViecService) => {
    expect(service).toBeTruthy();
  }));
});
