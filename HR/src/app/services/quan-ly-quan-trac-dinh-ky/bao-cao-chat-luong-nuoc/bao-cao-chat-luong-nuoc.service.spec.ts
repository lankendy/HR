/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { BaoCaoChatLuongNuocService } from './bao-cao-chat-luong-nuoc.service';

describe('Service: BaoCaoChatLuongNuoc', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [BaoCaoChatLuongNuocService]
    });
  });

  it('should ...', inject([BaoCaoChatLuongNuocService], (service: BaoCaoChatLuongNuocService) => {
    expect(service).toBeTruthy();
  }));
});
